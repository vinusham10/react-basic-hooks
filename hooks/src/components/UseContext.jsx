import React, { useContext } from "react";
import { ToggleTheme } from "../App";

function UseContext({ handleLike }) {
  const theme = useContext(ToggleTheme);

  return <>
  </>;
}

export default UseContext;